<nav class="navbar-default navbar-static-side" role="navigation">
    <div class="sidebar-collapse">
        <ul class="nav metismenu" id="side-menu">
            <li class="nav-header">
                <div class="dropdown profile-element">
                    <a data-toggle="dropdown" class="dropdown-toggle" href="#">
                        <span class="block m-t-xs font-bold">Username</span>
                        <span class="text-muted text-xs block">Title <b class="caret"></b></span>
                    </a>
                    <ul class="dropdown-menu animated fadeInRight m-t-xs" style="position: absolute; top: 91px; left: 0px; will-change: top, left;">
                        <li><a class="dropdown-item" href="#">Logout</a></li>
                    </ul>
                </div>
                <div class="logo-element">
                    TMLTH
                </div>
            </li>

            <li class="@Html.IsSelected(controller: "Home")">
                <a href="#"><i class="fa fa-bar-chart-o"></i> <span class="nav-label" data-i18n="nav.forms">Time sheet</span><span class="fa arrow"></span></a>
                <ul class="nav nav-second-level collapse @Html.IsSelected(controller: "Graphs", cssClass: "in")">
                    <li class="@Html.IsSelected(action: "Flot")"><a href="@Url.Action("index", "TimeSheets")">Log timesheet</a></li>
                    <li class="@Html.IsSelected(action: "Morris")"><a href="@Url.Action("#", "Home")">Dashbaord</a></li>
                </ul>
            </li>

            <li class="@Html.IsSelected(controller: "Home")">
                <a href="#"><i class="fa fa-server"></i> <span class="nav-label" data-i18n="nav.forms">Master setup</span><span class="fa arrow"></span></a>
                <ul class="nav nav-second-level collapse @Html.IsSelected(controller: "Graphs", cssClass: "in")">
                    <li class="@Html.IsSelected(action: "Flot")"><a href="@Url.Action("index", "Channel")">Channels</a></li>
                    <li class="@Html.IsSelected(action: "Morris")"><a href="@Url.Action("index", "Department")">Departments</a></li>
                    <li class="@Html.IsSelected(action: "Rickshaw")"><a href="@Url.Action("index", "Activities")">Activities</a></li>
                    <li class="@Html.IsSelected(action: "Chartjs")"><a href="@Url.Action("index", "Projects")">Projects</a></li>
                    <li class="@Html.IsSelected(action: "Chartist")"><a href="@Url.Action("Chartist", "Graphs")">Accounts</a></li>
                    <li class="@Html.IsSelected(action: "C3charts")"><a href="@Url.Action("index", "EmployeeType")">Employee type</a></li>
                    <li class="@Html.IsSelected(action: "Peity")"><a href="@Url.Action("index", "Jobtype")">Job type</a></li>
                    <li class="@Html.IsSelected(action: "Sparkline")"><a href="@Url.Action("index", "Roles")">Roles</a></li>
                    <li class="@Html.IsSelected(action: "Sparkline")"><a href="@Url.Action("index", "SectionDept")">Section</a></li>
                </ul>
            </li>
 
        </ul>
    </div>
</nav>